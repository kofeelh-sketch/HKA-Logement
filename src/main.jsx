import React from "react";
import ReactDOM from "react-dom/client";
import HkaCourtage from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HkaCourtage />
  </React.StrictMode>
);
